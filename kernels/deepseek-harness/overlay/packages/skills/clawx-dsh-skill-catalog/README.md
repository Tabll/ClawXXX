# @clawx/dsh-skill-catalog

Projects canonical ClawX Skills into DeepSeek Harness `ctx.skills` without
creating native durable history or linking OpenClaw/DSH filesystem roots.
ClawX SQLite and the canonical package store remain authoritative; this catalog
is rebuilt by the host reconciler whenever the runtime process starts.
